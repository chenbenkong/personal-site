# 2026-08-13

## 任务：搭建并部署"美观"的个人编程网站（GitHub Pages）

用户 chenbenkong（GitHub 用户名）要一个免服务器的、特别美观的个人编程网站，部署到 GitHub Pages。
最终站点：**https://chenbenkong.github.io/personal-site/** ；仓库：https://github.com/chenbenkong/personal-site （public, main 分支, Actions 自动部署）

### 关键决策（用户反馈）
- 第一版我"从零手写" Astro 站点，用户嫌丑、且明确要"找别人做好的模板改一下"。
- 改用 **saadpasta/developerFolio**（6.6k★，圈内最经典的"fork 后只改 `src/portfolio.js` 一个文件"的美观 React/CRA 模板）。
- 备选比对：bchiang7/v4（8.2k★ 但定制成本高：名字散落多 JSX + 40+ 项目要清理）；satnaing/dev-portfolio（仓库名不存在）；timlrx/tailwind-nextjs-starter-blog（偏博客）。
- 仓库已整体替换为 developerFolio；定制写在 `src/portfolio.js`（计算机学生"Benkong"占位信息，用户需替换真实学校/经历/图片）。

### 部署要点（CRA → GitHub Pages，已写入 .github/workflows/deploy.yml）
- Node 20 + OpenSSL3 → 构建需 `NODE_OPTIONS=--openssl-legacy-provider`。
- CI 下 react-scripts build 会把 lint 警告当错误 → 用 `CI=false npm run build`。
- package.json `homepage` 设为站点 URL，构建产物资源前缀自动为 /personal-site/。
- 用 `actions/deploy-pages`；Pages 源=GitHub Actions（build_type:workflow，已 `gh api POST /repos/.../pages -f build_type=workflow` 启用）。

### 环境踩坑（重要，避免重蹈）
- **GitHub MCP 连接器无建库权限**：`mcp__github__create_repository` 报 403（integration）。解决：本地装 `gh` CLI（winget 安装到 `C:\Program Files\GitHub CLI\gh.exe`，需 `export PATH="$PATH:/c/Program Files/GitHub CLI"`），`gh auth login --web` 设备流授权，再 `gh repo create` + git push。
- **安全删除策略会延迟执行**：`rm -rf` 大目录被 SAFE_DELETE 拦截并升级批准，批准后可能在后续命令才真正执行，把我刚 cp 回的文件又删掉（包括 .workbuddy 记忆目录）。
  - 对策：克隆模板到仓库内 `tmpl/`，改完用"按文件名精准删旧文件 + cp tmpl 内容"替换；`tmpl/`、`v4-tmp/` 等写入 .gitignore；避免批量 rm。
  - 注意：手写/追加的 .gitignore 规则和 deploy.yml，若在"延迟 rm → 重新 cp tmpl"之间，会被模板原版覆盖，需重新写入并验证。

## 后续可做（用户潜在需求）
- 把 portfolio.js 里的学校/实习/项目换成真实信息；替换 education/work 里的 logo 图片（现用模板占位图）。
- 想换风格可换 bchiang7/v4 或 astrofy；想加博客写作可保留 developerFolio 的 blogSection（现为硬编码）。
- 本地预览：`npm start`（CRA dev server）。
